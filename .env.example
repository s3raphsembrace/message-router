# Copy to .env and fill in. .env is gitignored; never commit real keys.

# Media interpretation (Layer 1). Any one of these is accepted.
GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_GENAI_API_KEY=

# Optional: override the model used for audio transcription and image OCR.
# MEDIA_MODEL=gemini-2.5-flash
